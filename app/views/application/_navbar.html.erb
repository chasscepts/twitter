<div class="col">
  <nav class="navbar navbar-expand-lg navbar-light bg-info">
    <div class="container-fluid">
      <a class="navbar-brand text-white" href="#"><h1>Twitter</h1></a>
      <div>
        <% if user.nil? %>
          <a href="" class="btn btn-success text-light">Log In</a>
          <a href="" class="btn btn-danger text-light">Register</a>
        <% else %>
          <span class="text-light d-inline-block me-3"> <%=user.name %></span>
          <a href="" class="text-danger">Logout</a>
        <% end %>
      </div>
    </div>
  </nav>
</div>
